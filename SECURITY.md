# Security

本工具只应从你信任的源码构建或直接运行源码。

- 不读取或上传账号令牌。
- 不修改 ChatGPT 安装目录、`auth.json` 或服务器权限。
- 临时调试端口仅绑定 `127.0.0.1:9229`，完成后由目标进程关闭。
- 仅在状态严格匹配 `denied / missing-account` 时修复；其他拒绝原因不会绕过。

如果你发现安全问题，请不要公开附带令牌、账号 ID 或日志中的个人路径。
